import React from 'react';

const MissedMedication = () => {
    return (
        <div>
            <>This here displays the history for the last month of missed dosages. if no dosages are missed in the last
                month it displays an all good message
            </>
        </div>
    );
};

export default MissedMedication;
