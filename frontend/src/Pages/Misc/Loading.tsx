import React from 'react';
import {Typography} from "@mui/material";

const Loading = () => {
    return (
        <div>
            <Typography>
                Loading...
            </Typography>
        </div>
    );
};

export default Loading;
