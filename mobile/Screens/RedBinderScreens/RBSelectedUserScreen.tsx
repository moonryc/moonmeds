import React from 'react';

import {Text, View} from 'react-native';

const RBSelectedUserScreen = () => {
    return (
        <Text>
            SELECTED USER SCREEN
        </Text>
    );
};

export default RBSelectedUserScreen;
